[project]
name = "poly"
version = "0.1.0"
description = "Polymarket prediction engine — macro & finance markets"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
